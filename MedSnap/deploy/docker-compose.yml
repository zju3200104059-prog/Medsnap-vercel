services:
  medsnap:
    container_name: medsnap
    build:
      context: ..
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "127.0.0.1:7860:7860"
    restart: unless-stopped
